import { Settings } from "lucide-react-native";
import { PlaceholderScreen } from "../../../lib/placeholder-screen";

export default function SettingsScreen() {
  return <PlaceholderScreen icon={Settings} title="Instellingen" subtitle="Openingstijden, zaakgegevens en meer" />;
}
